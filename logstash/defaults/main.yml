---
# defaults file for elk-connection/server

elastic:
  host: "127.0.0.1"
  port: "9200"
  index: "%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"

logstash:
  config_path: "/etc/logstash/conf.d/"

